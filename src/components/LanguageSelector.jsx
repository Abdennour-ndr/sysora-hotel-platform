import React from 'react';

const LanguageSelector = () => {
  // Language selector temporarily disabled - English only
  // Will be re-enabled when other languages are properly localized
  return null;
};

export default LanguageSelector;
