<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:statusBarColor">@color/sysora_light</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="colorPrimary">@color/sysora_mint</item>
        <item name="colorPrimaryDark">@color/sysora_midnight</item>
        <item name="colorAccent">@color/sysora_mint</item>
    </style>

    <!-- Splash screen theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/sysora_mint</item>
        <item name="android:navigationBarColor">@color/sysora_mint</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
